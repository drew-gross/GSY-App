<?xml version="1.0" encoding="UTF-8"?>
<Response>
    <Gather action="http://drew.pagekite.me/requesttype" method="POST" timeout="10" finishOnKey="*">
        <Say>
            Press one to report data.
			Press two to get data.
			Press star to end the call.
        </Say>
    </Gather>
    <Say>We didn't receive any input. Goodbye!</Say>
</Response>